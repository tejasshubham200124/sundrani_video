// export const storeChannelId = (channelId) => {
//     sessionStorage.setItem('id', JSON.stringify(channelId));
//   };
  
//   export const getChannelId = () => {
//     const userStr = sessionStorage.getItem('id');
//     if (userStr) return JSON.parse(userStr);
//     else return null;
//   };
  